spring:
  main:
    allow-circular-references: true
  datasource:
    #url: "jdbc:postgresql://localhost:5432/SpringJDBC"
    url: jdbc:h2:mem:testdb
    #username: "postgres"
    #password: "123"
  sql:
    init:
      mode: always


  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: ERROR